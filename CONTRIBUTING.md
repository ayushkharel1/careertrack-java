# Contributing

Use JDK 17+ and Maven 3.6.3+. Keep changes focused and explain the user-visible behavior. Run `mvn clean verify` before submitting a pull request. Add meaningful tests for changed backend behavior. Test keyboard navigation and narrow-screen layouts when changing the UI.

Do not commit `data/`, credentials, real job application notes, build output, or editor settings. Demo opportunities must remain clearly fictional.
